import { DarkModeClickDirective } from './dark-mode-click.directive';

describe('DarkModeClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkModeClickDirective();
    expect(directive).toBeTruthy();
  });
});
