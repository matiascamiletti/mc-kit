import { MCTdTemplateDirective } from './td-template.directive';

describe('TdTemplateDirective', () => {
  it('should create an instance', () => {
    //const directive = new TdTemplateDirective();
    //expect(directive).toBeTruthy();
  });
});
