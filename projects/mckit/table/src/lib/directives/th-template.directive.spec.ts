import { MCThTemplateDirective } from './th-template.directive';

describe('MCThTemplateDirective', () => {
  it('should create an instance', () => {
    //const directive = new MCThTemplateDirective();
    //expect(directive).toBeTruthy();
  });
});
